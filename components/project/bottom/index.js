import {observer,inject} from 'mobx-react';
import {
    bottomPanel,
    bottom,
    bottomLaunchBottons,
    filebrowserButton,
    fileHistory,
    fileHistoryItem,
    commitButton,
    launchButton
} from '../../../styles/mainUi.less';